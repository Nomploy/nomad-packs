# Changelog

## 0.1.0

- Initial release: Prometheus Node Exporter (host metrics for Prometheus) as a host-networked Nomad service with a
  read-only host bind, host PID namespace, a configurable listen port, an HTTP health check, and Nomad service
  discovery.
