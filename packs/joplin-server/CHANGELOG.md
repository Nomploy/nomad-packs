# Changelog

## 0.1.0

- Initial release: Joplin Server (self-hosted sync backend for Joplin) as an all-in-one host-networked
  Nomad job — a PostgreSQL prestart sidecar with a persistent data volume plus the Joplin Server app,
  configurable port and base URL, and Nomad service discovery. Migrations run automatically on boot.
