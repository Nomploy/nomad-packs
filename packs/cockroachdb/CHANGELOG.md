# Changelog

## 0.1.0

- Initial release: CockroachDB single-node (insecure) as a host-networked Nomad service with a
  data volume, the DB Console, configurable SQL/HTTP ports, and Nomad service discovery.
