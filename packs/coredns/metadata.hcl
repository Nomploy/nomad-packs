app {
  url = "https://coredns.io/"
}

pack {
  name        = "coredns"
  description = "CoreDNS — a fast, pluggable DNS server (the Kubernetes default) configured with a simple Corefile. Runs as a caching forwarder out of the box. Deployed as a single host-networked Nomad service."
  url         = "https://github.com/Nomploy/nomad-packs/tree/main/packs/coredns"
  version     = "0.1.0"
}
