# Changelog

## 0.1.0

- Initial release: Prometheus Blackbox Exporter as a stateless host-networked Nomad service
  with a rendered module config (http/tcp/icmp by default), a configurable listen port, and
  Nomad service discovery.
