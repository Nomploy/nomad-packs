# Changelog

## 0.1.0

- Initial release: Node-RED as a host-networked Nomad service with a prestart chown so it
  (uid 1000) can write its persistent volume, a configurable port (PORT) and timezone, and
  Nomad service discovery.
