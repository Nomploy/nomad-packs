# Changelog

## 0.1.0

- Initial release: Meilisearch as a host-networked Nomad service with a persistent Docker
  volume, a configurable HTTP port, optional master key (production mode with auth, or
  open development mode when unset), and Nomad service discovery.
