# Changelog

## 0.1.0

- Initial release: Restic REST Server as a single host-networked Nomad service with a data
  volume, optional HTTP basic auth, append-only mode (default on), a configurable port, extra
  server flags, and Nomad service discovery.
