# Changelog

## 0.1.0

- Initial release: immudb (immutable, cryptographically-verifiable database) as a host-networked Nomad
  service with DB + web-console ports, a persistent /var/lib/immudb volume, admin password + web console
  via env, and Nomad service discovery.
