# Changelog

## 0.1.0

- Initial release: Prometheus MySQLd Exporter as a stateless host-networked Nomad service using
  the v0.15+ interface (`--mysqld.address` / `--mysqld.username` + `MYSQLD_EXPORTER_PASSWORD`),
  a configurable listen port, and Nomad service discovery.
