# Changelog

## 0.1.0

- Initial release: LittleLink Server (self-hosted link-in-bio / Linktree alternative) as a host-networked,
  stateless Nomad service configured via env (name, theme, description, avatar, social links), with a
  scalable `count` and Nomad service discovery.
