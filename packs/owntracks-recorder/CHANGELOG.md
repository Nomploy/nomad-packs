# Changelog

## 0.1.0

- Initial release: OwnTracks Recorder (self-hosted location history + map) as a host-networked Nomad
  service in HTTP mode (MQTT disabled via OTR_PORT=0) with a persistent /store volume and Nomad service
  discovery.
