# Changelog

## 0.1.0

- Initial release: Jupyter (JupyterLab / Notebook) as a host-networked Nomad service with token
  auth via JUPYTER_TOKEN, a configurable bind port, a persistent work volume with a prestart
  chown for the jovyan user, and Nomad service discovery.
