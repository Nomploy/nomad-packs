# Changelog

## 0.1.0

- Initial release: Donetick (self-hosted tasks & chores manager) as a host-networked Nomad service using
  SQLite with persistent data + config volumes, DT_ENV=selfhosted, JWT secret and port via env, and Nomad
  service discovery.
