# Changelog

## 0.1.0

- Initial release: cAdvisor (per-container metrics with a Prometheus endpoint) as a host-networked Nomad service
  with read-only host/Docker bind mounts, a configurable port, an HTTP health check, and Nomad service discovery.
