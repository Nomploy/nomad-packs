# Changelog

## 0.1.0

- Initial release: ClickHouse server as a host-networked Nomad service with a persistent
  Docker volume, configurable HTTP and native ports (via a config.d override), a bootstrap
  user/database/password, a raised open-files ulimit, and Nomad service discovery.
