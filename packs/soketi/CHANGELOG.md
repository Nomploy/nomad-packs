# Changelog

## 0.1.0

- Initial release: soketi (Pusher-compatible WebSocket server) as a stateless host-networked Nomad
  service with configurable ports and default app credentials, plus a metrics endpoint and Nomad
  service discovery.
