# Changelog

## 0.1.0

- Initial release: Prometheus Postgres Exporter as a stateless host-networked Nomad service
  with a configurable DSN (`DATA_SOURCE_NAME`), a configurable listen port, and Nomad service
  discovery.
