IT Tools deployed as job "[[ var "job_name" . ]]" (host-networked on port [[ var "port" . ]]).

Open:      http://<node-ip>:[[ var "port" . ]]
Discovery: Nomad service "[[ var "job_name" . ]]" (provider=nomad)

Stateless — no volume. A grab-bag of offline dev/ops utilities (encoders, converters,
generators, formatters, network helpers, …). Everything runs client-side in the browser.
