# Changelog

## 0.1.0

- Initial release: Umami as an all-in-one, host-networked Nomad job with its PostgreSQL
  database as a prestart sidecar. Persistent named volume for Postgres, configurable port,
  optional app secret, and Nomad service discovery. Umami runs schema migrations on start.
