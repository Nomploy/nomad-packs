# Changelog

## 0.1.0

- Initial release: Gotenberg (stateless PDF/document conversion API) as a host-networked Nomad
  service with a configurable `--api-port`, an HTTP health check, a scalable `count`, and Nomad
  service discovery.
