# Changelog

## 0.1.0

- Initial release: TimescaleDB (PostgreSQL + TimescaleDB extension) as a host-networked Nomad
  service with a persistent Docker named volume, configurable database/user/password and port,
  and Nomad service discovery.
