# Changelog

## 0.1.0

- Initial release: Glances as a stateless host-networked Nomad service in web mode
  (`pid_mode = host`, optional read-only Docker socket), a configurable port, and Nomad service
  discovery.
