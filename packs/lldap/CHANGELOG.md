# Changelog

## 0.1.0

- Initial release: LLDAP (light LDAP server with a web UI) as a host-networked Nomad service using
  SQLite with a persistent /data volume, configurable web/LDAP ports, base DN, admin password, and JWT
  secret via env, and Nomad service discovery.
