# Changelog

## 0.1.0

- Initial release: Vaultwarden as a host-networked Nomad service with a persistent Docker
  volume for /data (bundled SQLite), configurable port/domain, a signups toggle, an
  optional admin-page token, and Nomad service discovery.
