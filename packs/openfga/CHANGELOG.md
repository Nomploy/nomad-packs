# Changelog

## 0.1.0

- Initial release: OpenFGA (Zanzibar-style authorization engine) as a host-networked Nomad service using an
  embedded SQLite datastore with a persistent volume, a prestart schema migration, HTTP/gRPC/playground ports,
  and Nomad service discovery.
