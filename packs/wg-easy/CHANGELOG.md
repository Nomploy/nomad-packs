# Changelog

## 0.1.0

- Initial release: wg-easy (WireGuard VPN with a web UI) as a host-networked Nomad service with NET_ADMIN +
  SYS_MODULE, IP-forwarding sysctls, a persistent /etc/wireguard volume, WG_HOST/PASSWORD/ports via env, and Nomad
  service discovery.
