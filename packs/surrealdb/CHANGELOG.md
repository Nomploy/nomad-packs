# Changelog

## 0.1.0

- Initial release: SurrealDB as a host-networked Nomad service with RocksDB storage on a
  persistent Docker volume, bootstrap root credentials, a configurable bind port, and Nomad
  service discovery.
