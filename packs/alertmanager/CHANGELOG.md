# Changelog

## 0.1.0

- Initial release: Prometheus Alertmanager as a host-networked Nomad service with a
  configurable rendered config (default: a minimal no-op receiver), a persistent volume for
  silences/notification log, a configurable listen port, and Nomad service discovery.
