# Changelog

## 0.1.0

- Initial release: RedisInsight as a host-networked Nomad service with a prestart chown so it
  (uid 1000) can write its persistent volume, a configurable listen port (RI_APP_PORT), and
  Nomad service discovery.
