# Changelog

## 0.1.0

- Initial release: Gatus as a stateless host-networked Nomad service with its config
  rendered from variables (web.port from `port`, `endpoints` section from a variable), and
  Nomad service discovery.
