# Changelog

## 0.1.0

- Initial release: MinIO (S3-compatible object storage) as a host-networked Nomad service with
  configurable API + console ports, root credentials via MINIO_ROOT_USER / MINIO_ROOT_PASSWORD, a
  persistent /data volume, and Nomad service discovery.
