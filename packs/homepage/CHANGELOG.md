# Changelog

## 0.1.0

- Initial release: Homepage as a host-networked Nomad service with a persistent Docker
  volume for its YAML config, configurable port (PORT), an allowed-hosts variable
  (HOMEPAGE_ALLOWED_HOSTS), and Nomad service discovery.
