# Changelog

## 0.1.0

- Initial release: Consul single-node server (with UI) as a host-networked Nomad service with a
  busybox chown init, a data volume, configurable HTTP/DNS ports, and Nomad service discovery.
