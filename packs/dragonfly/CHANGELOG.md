# Changelog

## 0.1.0

- Initial release: DragonflyDB as a host-networked Nomad service with the required memlock
  ulimit, snapshots on a persistent Docker volume, a configurable port, an optional password,
  and Nomad service discovery.
