app {
  url = "https://www.cockroachlabs.com/docs/"
}

pack {
  name        = "cockroachdb"
  description = "CockroachDB — a distributed SQL database that speaks the PostgreSQL wire protocol, with strong consistency and horizontal scale. Deployed here as a single-node, insecure instance for dev/homelab, host-networked with a data volume and the built-in DB Console."
  url         = "https://github.com/Nomploy/nomad-packs/tree/main/packs/cockroachdb"
  version     = "0.1.0"
}
