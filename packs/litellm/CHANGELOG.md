# Changelog

## 0.1.0

- Initial release: LiteLLM proxy as a stateless host-networked Nomad service with a rendered
  config.yaml (proxies the ollama pack by default), a master key, a configurable port, and Nomad
  service discovery.
