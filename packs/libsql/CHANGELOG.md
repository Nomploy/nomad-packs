# Changelog

## 0.1.0

- Initial release: libSQL server (sqld, network SQLite by Turso) as a host-networked Nomad service running
  a primary node with a persistent /var/lib/sqld volume, configurable HTTP listen port, optional JWT auth,
  and Nomad service discovery.
