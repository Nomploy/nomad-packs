app {
  url = "https://docs.rsshub.app/"
}

pack {
  name        = "rsshub"
  description = "RSSHub — an extensible RSS feed generator that creates feeds for thousands of sites that don't offer their own. Runs standalone with in-memory caching. Deployed as a single stateless host-networked Nomad service."
  url         = "https://github.com/Nomploy/nomad-packs/tree/main/packs/rsshub"
  version     = "0.1.0"
}
