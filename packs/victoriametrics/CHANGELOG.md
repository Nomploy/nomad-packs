# Changelog

## 0.1.0

- Initial release: VictoriaMetrics (single-node) as a host-networked Nomad service with a
  persistent Docker volume for its time-series storage, a configurable HTTP port and
  retention period, and Nomad service discovery.
