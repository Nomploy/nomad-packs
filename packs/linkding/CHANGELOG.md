# Changelog

## 0.1.0

- Initial release: linkding (self-hosted bookmark manager) as a host-networked Nomad service using
  SQLite with a persistent /etc/linkding/data volume, LD_SERVER_PORT + LD_SUPERUSER_* env for the
  initial admin, and Nomad service discovery.
