# Changelog

## 0.1.0

- Initial release: PostgREST (instant REST API over PostgreSQL) as a host-networked, stateless Nomad
  service with configurable DB URI, exposed schema, anonymous role, optional JWT secret, a scalable
  `count`, and Nomad service discovery.
