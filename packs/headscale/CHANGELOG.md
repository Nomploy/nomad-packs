# Changelog

## 0.1.0

- Initial release: Headscale as a host-networked Nomad service with a rendered config.yaml
  (SQLite, embedded DERP disabled), a data volume, configurable server URL / ports, and Nomad
  service discovery. Image pinned to 0.23.0 (config schema is version-specific).
