# Changelog

## 0.1.0

- Initial release: Glance as a stateless host-networked Nomad service with its config rendered
  from variables (server host/port from `port`, `pages` section from a variable), and Nomad
  service discovery.
